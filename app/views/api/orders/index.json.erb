
<%#= sanitize collection.decorate.to_json %>
<%= sanitize ({ collection: collection.decorate(context: {short_view: true}).as_json, total_pages: collection.total_pages, current_page: collection.current_page }).to_json %>
